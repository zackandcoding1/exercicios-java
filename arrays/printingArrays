import java.util.Arrays;

public class array {
    static void main() {
        String[] nomes = {"João", "Rosana", "Zé"};
        /*for (int i = 0; i < nomes.length; i++) {
            System.out.println(nomes[i]);
        }*/
        
        for (String nome : nomes) {
            System.out.println(nome);
        }
    }
    
}
